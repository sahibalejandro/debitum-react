{
  "name": "debitum-constants",
  "version": "0.0.0",
  "description": "Constants",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc src/index.ts --declaration --outDir dist",
    "watch": "yarn build --watch"
  },
  "dependencies": {
    "typescript": "^3.8.3"
  }
}
